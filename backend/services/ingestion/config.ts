/**
 * Hardcoded Profile Configuration
 * Update these credentials with your LeetCode and Codeforces IDs.
 */
export const DEFAULT_USER_CONFIG = {
  userId: process.env.USER_ID || "pragatighosh25",
  leetcodeUsername: process.env.LEETCODE_USERNAME || "pragatighosh25",
  codeforcesHandle: process.env.CODEFORCES_HANDLE || "pragatighosh",
  autoFetchOnStartup: true,
};
