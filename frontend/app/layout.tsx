import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  subsets: ["latin"],
  variable: "--font-geist-sans",
  display: "swap",
});

const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: "BlindSpot",
  description:
    "Analyze LeetCode & Codeforces submissions, detect recurring algorithmic failure patterns, and eliminate blind spots with targeted spaced repetition.",
  icons: {
    icon: "/icon.svg",
    shortcut: "/icon.svg",
    apple: "/icon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} dark`}>
      <body className="antialiased bg-[#0A0A0A] text-[#FAFAF8] min-h-screen font-sans selection:bg-[#E4007C] selection:text-white">
        {children}
      </body>
    </html>
  );
}
